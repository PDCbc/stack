hubdb:
    image: mongo
    hostname: hubdb
    restart: always
    environment:
        - LC_ALL=C # Addresses a mongo language bug

auth:
    build: auth/.
    hostname: auth
    restart: always
    env_file:
        - ../config.env
